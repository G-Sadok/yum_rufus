//
// Mesures de l ecran Parcourir, section 5.3.
//

extension Jetons {
    /// Ecran Parcourir, section 5.3.
    public enum Parcourir {
        /// Marge laterale, celle des lignes de reglage.
        public static let margeLaterale = LigneDeReglage.margeLaterale

        /// Compteur pose au dessus de la liste, en `headline`.
        public static let compteur = Typo.headline

        /// Nom d une source dans la liste.
        public static let nom = Typo.body
    }
}
