/// Marqueur du paquet ImagePipeline.
///
/// ImagePipeline porte le decodage sous echantillonne, les caches et les
/// traitements d image. Le decodage arrive avec la fonctionnalite F010.
public enum DomaineImagePipeline {
    /// Nom du domaine, utilise par les diagnostics et par les tests de frontiere.
    public static let nom = "ImagePipeline"
}
